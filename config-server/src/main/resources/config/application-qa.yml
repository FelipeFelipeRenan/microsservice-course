spring:
  config:
    import: "optional:configserver:"
  application:
    name: greeting-service
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'

greeting-service:
  greeting: "Ol�"
  default-value: "Mundo"
